<%= form_with(model: startseite) do |form| %>
  <% if startseite.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(startseite.errors.count, "error") %> prohibited this startseite from being saved:</h2>

      <ul>
        <% startseite.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :angeforderteDaten, style: "display: block" %>
    <%= form.text_field :angeforderteDaten %>
  </div>

  <div>
    <%= form.label :letzteProzesse, style: "display: block" %>
    <%= form.text_field :letzteProzesse %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
